# react-helmet-async@2.0.5 declares peer react ^16.6||^17||^18 but is
# runtime-compatible with React 19 (no React internals touched). Without this
# flag a clean `npm ci` cannot reproduce the deployed bundle at all.
# Remove when the helmet dependency is upgraded/replaced.
legacy-peer-deps=true
