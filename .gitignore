# Environment and OS noise
.venv/
env/
__pycache__/
*.pyc
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (contain secrets - NEVER commit)
.env
.env.*
!.env.example
.env.local

# Large Data Files (Ignored - except compressed production data)
*.csv
*.db
!prod_data_clean.csv.gz
MOT Test Results/
MOT Test Failures/
MOT testing data results (2024)-2/

# Test results and profiling data
*_results*.json
load_test_results.json
performance_audit*.json
*.prof

# ExperimentLoop scratch (CatBoost training logs from score_core tests/runs)
catboost_info/

# ExperimentLoop dev-grade run artifacts (the canonical evidence is the TRACKED
# promotion_manifests/ + promotions.tsv; dev runs are diagnostic-only and not tracked)
experiment_loop/runs/
experiment_loop/ledger.tsv

# Reference documents (PDFs)
*.pdf

# Node.js / Frontend
node_modules
dist
dist-ssr
*.local

# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
